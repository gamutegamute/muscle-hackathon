# Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Optional admin API token for protected maintenance endpoints.
# Leave unset locally unless you need to call admin-only endpoints manually.
ADMIN_API_TOKEN=your_admin_api_token_here

# 動作確認チェックリスト

## バックエンド起動確認

- `GET /health` が `ok` を返す
- `GET /` が応答する

## プロフィール

- 初回プロフィール保存ができる
- 設定画面で身長・体重・体脂肪を保存できる

## 記録

- 手動入力で記録を保存できる
- タイマー結果を保存できる
- ストップウォッチ結果を保存できる
- ホームの今日の記録一覧に表示される

## 記録編集

- 今日の記録をタップして編集画面に入れる
- 同じ記録を 50 分から 40 分に直したとき、90 分にならない
- 保存後にホームの累計時間が更新される

## 集計

- ホームの累計時間が出る
- ホームの連続記録が出る
- カレンダーに記録日が反映される
- グラフ画面に記録分数が反映される

## 接続

- フロントからバックエンド停止時に保存失敗が分かる
- `.env` の API URL を変えると接続先を切り替えられる
